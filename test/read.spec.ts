import * as chai from 'chai';
const { expect } = chai;

describe('class 读取', () => {
    it(``, async () => {
    });

    before(() => {
        console.log('===读取 测试开始====');
    });

    after(() => {
        console.log('===读取 测试完成====');
    });
});
